import { Text, View } from 'react-native'

const SearchHistoryPage = () => {
    return(
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center', }}>
            <Text>Search History Page</Text>
        </View>
    )
}

export default SearchHistoryPage